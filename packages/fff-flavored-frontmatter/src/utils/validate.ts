import type { FFFFlavoredFrontmatter } from '../types.ts'

/**
 * FFF Flavored Frontmatter Validator
 * @alpha
 * @param fm - FFF Flavored Frontmatter
 * @returns input fm
 */
export const validate = (fm: FFFFlavoredFrontmatter): FFFFlavoredFrontmatter => {
  return fm
}
